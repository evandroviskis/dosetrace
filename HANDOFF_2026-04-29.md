# DoseTrace — Handoff Prompt (continue on other machine)

Copy and paste everything below the divider into a new Claude / Cowork session on the other machine. Before you do, on the **MacBook side** run:

```bash
cd ~/Desktop/dosetrace
git add -A
git commit -m "Handoff 2026-04-29: OAuth packages installed, council R1+R2+amendment, preview build queued"
git push
```

Then on the **office machine**:

```bash
cd ~/Desktop/dosetrace
git pull
```

---

I'm Evandro. Continuing work on DoseTrace — a medication / protocol tracking mobile app. Local path: `~/Desktop/dosetrace`. Repo: https://github.com/evandroviskis/dosetrace.git (private). Work across two machines (this office desktop + a MacBook), git-synced.

## Project basics (unchanged)

- React Native + Expo SDK 54, Health & Fitness category
- Supabase (auth + cloud sync), SQLite (local), RevenueCat (purchases)
- 6 languages: EN, ES, PT, FR, DE, IT
- Bundle ID: `io.outcom.dosetrace`
- EAS Project ID: `4780e120-5f45-4482-8e95-b443929b6f8a`
- Expo account: `jootaerre` (hello@dosetrace.io)
- Apple ID for builds: `jootaerre@yahoo.com.br`
- Domain: dosetrace.io (GoDaddy)
- Supabase project: "dosesync" (EGSX dev org)
- **DoseTrace is owned by Outcom** — a separate company from EvoxBiolabs (peptide sales). The two companies are never publicly linked. Don't put EvoxBiolabs anywhere in the app, copy, or repo.

## What was done in the previous session (MacBook, 2026-04-29)

1. `npm install` ran clean (882 packages, 0 vulnerabilities). Node v24.15.0 LTS installed on the MacBook via nvm.
2. `npx expo install expo-auth-session expo-crypto expo-web-browser` upgraded the three OAuth packages from SDK 53 majors to SDK 54 majors:
   - `expo-auth-session 6.1.3 → 7.0.10`
   - `expo-crypto 14.1.3 → 15.0.8`
   - `expo-web-browser 15.0.4 → 15.0.10`
   - Added `expo-web-browser` to the `plugins` array in `app.json`. **package.json and app.json have committed changes from this.**
3. `npx expo-doctor` clean (17/17 checks pass).
4. `eas-cli` installed globally on the MacBook. Logged in as `jootaerre`.
5. **EAS preview build queued for iOS + Android.**
   - Android build: https://expo.dev/accounts/jootaerre/projects/dosetrace/builds/44304c85-e140-43a6-9f72-dea67253a381
   - iOS build: https://expo.dev/accounts/jootaerre/projects/dosetrace/builds/e6d7f29e-56b9-4646-9be4-cb0a749f893b
   - iPhone (UDID `00008120-000211513682601E`) registered in the ad-hoc provisioning profile. New cert serial `2A4B81B6CB6165E63BE37489EF7F7E81`, expires 2027-04-07.
   - **The build went out with no Supabase env vars baked in** — `eas.json` only has `EXPO_PUBLIC_SUPABASE_URL` / `EXPO_PUBLIC_SUPABASE_ANON_KEY` in the `production` profile, not `preview`. So even when this build installs, Supabase calls will fail.

## What's still broken / open

1. **Email auth broken (pre-existing).** Custom SMTP via GoDaddy, but MX records point to Google → outgoing mail rejected. Plan was to migrate SMTP to Resend, verify dosetrace.io, update Supabase SMTP settings, decide MX (GoDaddy vs Google Workspace). Not started.
2. **Google OAuth client code is in but Supabase Dashboard provider is not configured.** `lib/supabase.js` has `signInWithGoogle()` using `expo-auth-session` + `WebBrowser.openAuthSessionAsync` with `scheme: 'dosetrace'`. Needs Google Cloud OAuth credentials and Supabase Auth → Providers → Google config.
3. **Env vars decision unresolved.** Two paths: (A) patch `eas.json` to add `env` block to `preview` (and `development`) matching `production`'s — fastest, in git. (B) `eas env:create --environment preview --name EXPO_PUBLIC_SUPABASE_URL --value "..." --visibility plaintext` (and same for ANON_KEY) — server-side, cleaner, but invisible from the repo.
4. **Play Store closed-testing third submission pending.** Need 12+ testers opted in for 14 days. Currently 4 on list, 0 opted in. iOS not yet submitted at all.

## The council ran twice in the previous session

Two reports were saved to the repo. Read them before making product decisions:

- `council-report-2026-04-29-1855.html` — Round 1 (pressure-test the whole app idea)
- `council-report-2026-04-29-1855-round2.html` — Round 2 (V1 scope, competitive sweep, missing vs overthought) — has a banner pointing to the amendment
- `council-amendment-2026-04-29-1855.html` — Three corrections to Round 2 (vendor-conflict argument retracted; serum curve IS differentiated; 6-language launch is justified)
- `council-transcript-2026-04-29-1855.md` and `council-transcript-2026-04-29-1855-round2.md` — full transcripts

**The council's net V1 plan after the amendment:**

- **V1 (ship in 14-21 days):** reconstitution calculator (hero), dose log + reminders, vial tracker, ~20 compound presets, streaks, free 5 protocols / paid unlimited, **all 6 languages at launch**, email auth working, Android first / iOS within 7 days. No EvoxBiolabs mention anywhere.
- **V1.5 (30 days post-launch):** injection site rotation + body map (verify FDA/ANVISA/EU MDR first), custom symptom tracking, protocol sharing via link/QR.
- **V2 (1k active OR 100 paid subs):** bloodwork PDF upload, **personalized PK curve from bloodwork** (premium hero — overlaying actual bloodwork values on the user's own dosing timeline; this is the differentiated serum visualizer, not a generic chart), Apple Watch (only if iOS users specifically request), doctor-shareable PDF report.
- **V3 (5k active OR $5k MRR):** Apple Health / Google Fit, mood diary, persistent escalating alarms, barcode scanning.
- **V4 (probably never, requires legal counsel):** family / dependent profiles, voice reminders.
- **KILL:** drug interaction checker (regulatory cliff), vitamin causal-correlation in this app (different audience, different mental model — possible separate product later).

**Highest-leverage missing feature for V1:** ~20 one-tap compound presets (BPC-157, TB-500, semaglutide, tirzepatide, retatrutide, ipamorelin, CJC-1295, sermorelin, MOTS-c, NAD+, GHK-Cu, epitalon, testosterone enanthate, testosterone cypionate, HCG, T3, T4, B12, etc.). Codebase currently requires manual entry. Competitors like TRT Plus already have 80+ presets. This is the biggest visible install→active conversion lever.

## Distribution context (use sparingly, never publicly link to EvoxBiolabs)

- ~60 weekly peptide-buying customers (private network)
- 500-person consumer list
- 150-member WhatsApp channel
- Plan to use paid peptide-niche influencers (BRL/EUR markets are 3.5–4.5× cheaper than US)
- 6-language launch is intentional — matches existing customer-nationality distribution
- Goal: 20–50k users in 5 years. Income reinvested 100%.

## Working style

- Direct, practical answers. Show exact commands.
- When you need info from the repo, read the files — don't guess.
- Don't strip the 6 languages. Don't introduce EvoxBiolabs anywhere. Don't add a drug interaction checker.

## Please start by

1. Requesting access to the dosetrace folder via `request_cowork_directory` with path `~/Desktop/dosetrace`.
2. Running `git status` and `git log --oneline -10` so we can confirm the office machine is in sync with the latest push.
3. Running `eas build:list --limit 4` to see the status of the two builds queued from the MacBook (Android `44304c85…`, iOS `e6d7f29e…`). Tell me whether they finished, failed, or are still running.
4. Asking me which path I want to take next:
   - **A)** Finish the technical thread (patch env vars, install the preview build on iPhone, fix email auth, configure Google OAuth in Supabase Dashboard).
   - **B)** Pivot to the council's V1 scope work — start with the 20 compound presets data file.
   - **C)** Both in parallel.
